import 'package:flutter/material.dart';

class VerNota extends StatelessWidget {
  const VerNota({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Nota'),
      ),
    );
  }
}
